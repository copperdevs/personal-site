---
interface Props {
  status: Status;
}

const { status } = Astro.props;

import { Image } from "astro:assets";
import dogImage from "../../../public/favicon.png";
import { type Status } from "../../lib/types.ts";
---

<Image
  src={dogImage}
  alt="a picture of my dog"
  class:list={["dog-image", status.toString()]}
/>
