---
interface Props {
  iconType: IconType;
  iconLink?: string;
}

const { iconType, iconLink } = Astro.props;

import FontAwesomeIcon from "./FontAwesomeIcon.astro";
import { IconType } from "../../lib/types";
---

<FontAwesomeIcon iconName={iconType.toString()} targetUrl={iconLink} />
