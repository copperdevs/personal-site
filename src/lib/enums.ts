enum StackType {
  Software = "software",
  Hardware = "hardware",
  Langauge = "language",
}

enum SoftwarePlatform {
  Windows = "windows",
  Mac = "mac",
  Both = "both",
}

enum UrlType {
  Project = "project",
  Source = "source",
  Package = "package",
}

export { StackType, SoftwarePlatform, UrlType };
