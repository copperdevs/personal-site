enum StackType {
  Software = "software",
  Hardware = "hardware",
  Langauge = "language",
}

enum SoftwarePlatform {
  Windows = "windows",
  Mac = "mac",
  Both = "both",
}

export { StackType, SoftwarePlatform };
